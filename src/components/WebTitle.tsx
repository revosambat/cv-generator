import { Box, Typography } from "@mui/material"

const WebTitle = () => {
	return (
		<Box>
			<Typography sx={{ padding: "30px", width: "100%" }} variant="h1">
				<i>CV-Generator</i>
			</Typography>
		</Box>
	)
}

export default WebTitle
